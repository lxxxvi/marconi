language: ruby
addons:
  postgresql: "12.3"
cache:
  bundler: true
rvm:
  - 2.7.1
services:
  - postgresql
before_install:
  - "gem install bundler"
before_script:
  - psql -c 'create database travis_ci_test;' -U postgres
  - cp config/database.yml.travis config/database.yml
script:
  - "bin/test"


